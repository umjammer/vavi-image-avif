-I /usr/local/src/aom/aom

-root vavi.awt.image.jna

//-parseInOneChunk
-runtime JNA

//-o "$(DIR)/../java" // overwrite versioned files instead of writing to target/generated-sources/java

//-v
//-choicesOut "$(DIR)/rococoa.choices"

/usr/local/include/avif/avif.h